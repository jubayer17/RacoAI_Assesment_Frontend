# Local frontend → local backend (no proxy needed)
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
#
# Vercel / remote: browser calls /api/proxy/..., Next.js forwards to this URL
NEXT_PUBLIC_API_URL=https://nondepreciatively-metaleptic-anja.ngrok-free.dev
# Optional server-only alias (defaults to NEXT_PUBLIC_API_URL)
# BACKEND_URL=https://nondepreciatively-metaleptic-anja.ngrok-free.dev
